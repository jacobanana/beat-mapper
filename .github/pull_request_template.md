<!--
Written to be read on a phone, by someone who did not watch it happen and
will not check out the branch. Lead with what changed and why; the diff is
underneath if they want it.
-->

## What changed

<!-- Grouped by issue when the branch closes several:

**#NNN — the surface**
- …
-->

## Seen, not just green

<!-- `bash scripts/checks.sh` passes — and the screenshots, attached, at
     every width the change crosses. A visual change with no picture is not
     finished. Say what the shots show, including anything they caught. -->

## Notes

<!-- Anything deliberately left out, and why. Anything found on the way that
     is not fixed here. -->

<!--
One keyword per issue, one per line. `Closes #1, #2` links the first and
silently drops the rest; a bare `#1` links without ever closing. Both leave
issues open behind a merged PR.

Closes #NNN
Closes #NNN
Part of #NNN
-->
